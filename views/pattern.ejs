<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/index.css">
    <link rel="stylesheet" href="/css/pattern.css">
    <link href="https://fonts.googleapis.com/css2?family=Acme&display=swap" rel="stylesheet">
    <title>pattern</title>
</head>
<body>
    <header>
        <h1>Crochet Hub</h1>
        <nav>
            <div class="navitem">
                <input type="search" name="search" placeholder="search"/>
                <input type="submit" name="search"/>
            </div>
            <a class="navitem" href="/">Home</a>
            <div class="dropdown navitem">
                <button class="dropbtn">MENU</button>
                <div class="dropdown-content">
                    <a href="/pattern/create">Create Pattern</a>
                    <a href="/pattern/<%= pattern.patternType %>/<%= pattern.id %>/edit">Edit Pattern</a>
        </nav>
    </header>
    <main>
        <section id='pattern'>
            <section class='title'>
                <h2><%= pattern.title %></h2>
                <h4>Created by <a href="/member/profile/<%= pattern.memberId %>"><%= pattern.Member.first_name %></a></h4>
            </section>
            <section class='intro'>                   
                <img src='<%= pattern.introImg %>'>
                <p><%= pattern.introContent %></p>                
            </section>
            <section class='steps'>
                <div>
                    <h5>Before beginning, these are the items that will be needed:</h5>
                    <p><%= pattern.itemsNeeded %></p>
                </div>
                <div>
                    <h5>Ready to begin, pattern provided below:</h5>
                    <p><%= pattern.contentHowTo %></p>
                </div>
                <div>
                    <% if (pattern.Design.imgUrl) { %>
                    <h5>Once the pattern has been completed, will look similar to the image below:</h5>
                    <img src='<%= pattern.Design.imgUrl %>'>
                    <% } %>
                    <h5><%= pattern.endingContent %></h5>
                </div>
            </section>
        </section>
        <aside>
            <div id='creator'>
                <h5>About Author:</h5>
                <div>
                    <p><img src='<%= pattern.Member.Avatar.imgUrl %>'></p>
                    <p><%= pattern.Member.first_name %></p>
                </div>
                <p><%= pattern.Member.aboutMe %></p>
            </div>
        </aside>
<%- include('./template/footer') %>

